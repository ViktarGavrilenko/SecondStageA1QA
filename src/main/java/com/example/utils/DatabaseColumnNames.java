package com.example.utils;

public enum DatabaseColumnNames {
    name,
    status_id,
    method_name,
    project_id,
    session_id,
    start_time,
    end_time,
    env,
    browser,
    author_id
}
